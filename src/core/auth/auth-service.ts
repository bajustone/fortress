// TODO: Implement AuthService
// - login(identifier, password, meta?)
// - refresh(refreshToken, meta?)
// - logout(refreshToken)
// - me(userId)
// - createUser(data)
// - verifyToken(token)
// - signToken(claims)
export {};
