// TODO: Implement PasswordHasher interface + default WASM Argon2id
// - hash(password): Promise<string>
// - verify(hash, password): Promise<boolean>
export type { PasswordHasher } from '../config';
