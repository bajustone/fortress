// TODO: Implement refresh token generation, SHA256 hashing, rotation, family tracking
// - generateToken(): { raw, hash }
// - hashToken(raw): string
// - rotateToken(db, rawToken): { userId, newRawToken }
export {};
