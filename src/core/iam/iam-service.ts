// TODO: Implement IamService
// - checkPermission(userId, resource, action, context?)
// - getUserPermissions(userId)
// - createRole(name, permissions)
// - bindRole / bindRoleToUser / bindRoleToGroup
// - createGroup / addUserToGroup / removeUserFromGroup
// - syncResources(direction, filePath?)
export {};
