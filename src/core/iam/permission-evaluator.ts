// TODO: Implement permission evaluation
// - evaluatePermissions(permissions, resource, action, context?)
// - evaluateConditions(conditions, context)
// - resolveVariable(template, context) — resolves ${user.id} etc.
// - Supports 'allow-only' and 'deny-overrides' evaluation modes
export {};
