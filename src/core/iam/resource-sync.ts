// TODO: Implement fortress.resources.json sync
// - loadResourceFile(filePath): ResourceDefinition
// - pushToDatabase(db, resources): sync JSON → DB
// - pullFromDatabase(db, filePath): sync DB → JSON
// - generateTypes(resources, outputPath): generate fortress.resources.d.ts
export {};
