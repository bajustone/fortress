// TODO: Implement createTestAdapter()
// - Uses bun:sqlite in-memory database via Drizzle
// - Auto-creates tables from fortress schema
// - Returns a ready-to-use DatabaseAdapter
//
// Usage:
//   import { createTestAdapter } from '@bajustone/fortress/testing';
//   const fortress = createFortress({ database: createTestAdapter(), ... });

export {};
